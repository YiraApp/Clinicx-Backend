/**
 * Application Constants
 * (Environment-specific values should be added to .env or Azure Application Settings)
 */
export const CONSTANTS = {
    // Add non-environment specific constants here if needed
};
